# 🛠️ Tools Hub - مركز الأدوات

موقع أدوات إلكترونية بسيطة يوفر مجموعة من الأدوات المفيدة للمهام اليومية.

## ✨ الميزات

### 🎯 الأدوات المتاحة
- **محول الصور**: تحويل الصور بين صيغ مختلفة (JPG, PNG, WEBP, AVIF)
- **ضاغط الملفات**: ضغط ملفات متعددة في ملف ZIP واحد
- **استخراج النص (OCR)**: استخراج النص من الصور باستخدام تقنية OCR

### 🌟 الميزات الإضافية
- 🌙 **Dark/Light Mode**: دعم الوضع الداكن والفاتح
- 🌐 **i18n**: دعم اللغتين العربية والإنجليزية
- 📱 **PWA**: تطبيق يعمل بدون إنترنت (Progressive Web App)
- 🧩 **Server Actions / API Routes**: مسارات API و Server Actions جاهزة
- 🔐 **Authentication**: نظام توثيق باستخدام NextAuth
- 🎨 **Tailwind + Shadcn UI**: تصميم احترافي باستخدام Tailwind CSS و Shadcn UI

## 🚀 البدء السريع

### المتطلبات
- Node.js 18+ 
- pnpm (أو npm/yarn)

### التثبيت

```bash
# تثبيت الحزم
pnpm install

# إنشاء ملف .env
cp .env.example .env

# تعديل ملف .env وإضافة:
# NEXTAUTH_SECRET=your-secret-key
# GOOGLE_CLIENT_ID=your-google-client-id (اختياري)
# GOOGLE_CLIENT_SECRET=your-google-client-secret (اختياري)
```

### التشغيل

```bash
# وضع التطوير
pnpm dev

# البناء للإنتاج
pnpm build

# تشغيل الإنتاج
pnpm start
```

## 📁 هيكل المشروع

```
├── app/
│   ├── [locale]/          # الصفحات مع دعم i18n
│   │   ├── page.tsx      # الصفحة الرئيسية
│   │   ├── image-converter/
│   │   ├── file-compressor/
│   │   ├── ocr/
│   │   └── auth/
│   ├── api/              # API Routes
│   │   ├── auth/
│   │   ├── compress/
│   │   └── convert-image/
│   └── actions/          # Server Actions
├── components/
│   ├── ui/               # مكونات Shadcn UI
│   ├── header.tsx
│   └── theme-provider.tsx
├── i18n/                 # إعدادات i18n
├── messages/             # ملفات الترجمة
│   ├── ar.json
│   └── en.json
└── public/
    └── manifest.json     # ملف PWA
```

## 🔧 الإعدادات

### NextAuth
1. أنشئ ملف `.env` و أضف:
   ```
   NEXTAUTH_URL=http://localhost:3000
   NEXTAUTH_SECRET=your-secret-key
   ```

2. لتوليد `NEXTAUTH_SECRET`:
   ```bash
   openssl rand -base64 32
   ```

### Google OAuth (اختياري)
1. اذهب إلى [Google Cloud Console](https://console.cloud.google.com/)
2. أنشئ مشروع جديد
3. فعّل Google+ API
4. أنشئ OAuth 2.0 credentials
5. أضف في `.env`:
   ```
   GOOGLE_CLIENT_ID=your-client-id
   GOOGLE_CLIENT_SECRET=your-client-secret
   ```

### PWA Icons
أضف أيقونات PWA في مجلد `public/`:
- `icon-192.png` (192x192)
- `icon-512.png` (512x512)

## 🎨 التخصيص

### الألوان
يمكنك تخصيص الألوان في `app/globals.css`:

```css
:root {
  --primary: 221.2 83.2% 53.3%;
  /* ... */
}
```

### الترجمات
أضف ترجمات جديدة في `messages/ar.json` و `messages/en.json`

## 📝 ملاحظات

- **OCR**: يستخدم Tesseract.js لاستخراج النص من الصور (يدعم العربية والإنجليزية)
- **Image Conversion**: يتم التحويل في المتصفح (client-side) للأداء الأفضل
- **File Compression**: يستخدم JSZip لضغط الملفات
- **Authentication**: حساب تجريبي: `demo@example.com` / `demo123`

## 🛠️ التقنيات المستخدمة

- **Next.js 16** - إطار عمل React
- **TypeScript** - لغة البرمجة
- **Tailwind CSS** - تصميم CSS
- **Shadcn UI** - مكونات UI
- **NextAuth** - التوثيق
- **next-intl** - الترجمة
- **next-themes** - الوضع الداكن/الفاتح
- **Tesseract.js** - OCR
- **JSZip** - ضغط الملفات
- **next-pwa** - PWA

## 📄 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الحر.

## 🤝 المساهمة

نرحب بجميع المساهمات! يرجى فتح issue أو pull request.

---

صُنع بـ ❤️ باستخدام Next.js
